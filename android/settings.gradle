pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
    plugins {
        id 'com.android.library' version "${AGPVersion}" apply false
    }
}

rootProject.name = 'flutter_nfc_kit'
